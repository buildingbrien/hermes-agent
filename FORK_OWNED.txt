# Fork-owned overlay on vendor/hermes-upstream (v2026.9.7). See PHASE_B_OVERLAY.md.
# PATCHES (fold re-fits, in runtime-patches/series):
#   0001 deepseek prefix-cache · 0002 aux flash-guard · 0003 prompt_builder ·
#   0004 local PATH · 0005 default_soul identity · 0006 deepseek retired-ID safety net (v4-flash/vision-exp/*vision* -> deepseek-flash, in model_normalize AND on explicit aux models via auxiliary_client._fold_retired_deepseek_model) ·
#   0007 cron meeting · 0008 file_safety #53 denies · 0009 browser_cdp session-material ·
#   0010 browser_tool UI-access · 0011 toolsets (#54 desktop/board/fleet) ·
#   0012 cronjob grants · 0014 registry unavailable_out · 0015 process_registry reconcile ·
#   0016 sandbox call-sites   (0013 session-search DROPPED — obsolete upstream) ·
#   0017 cryptography intel wheel · 0018 tool-result dict normalize ·
#   0019 read_file extracted-doc not-found rescue (leading-space names; canary 2026-09-13) ·
#   0020 search_files fallback bounds (no-rg lane: -I/prune/size cap, native drain keeps
#        partials on timeout, iCloud dataless never read on either lane, honest timeout hint;
#        r2: EPERM-tolerant at-bound kill, root-anchored space-named hits, diagnostics never
#        spend the bound, ~/Library cloud routing, realpath rg anchoring, find -H + abs root)
# ADDONS (fork-only files, carried whole under runtime-addons/):
tools/fleet_send.py
tools/dial_meeting.py
tools/meeting_notes.py
tools/schedule_meeting_tool.py
tools/action_items_tool.py
tools/desktop_use.py
cron/meeting_join.py
tools/browser_cursor.py
tools/ask_agent_tool.py
tools/delegate_neith.py
tools/board_tool.py
tools/gbrain_tool.py
tools/email_send_tool.py
cron/p7_task_hook.py
sandbox_executor.py
sandbox_routing.py
tools/bridge_auth.py
tools/fleet_budget.py
tools/session_material_policy.py
tools/lucaryin_filelock.py
# DROPPED: environments/tool_call_parsers/* (RL-training-only), gemini_cloudcode_adapter
#   (multi-model, violates DeepSeek-lock), matrix.py, browser_providers/base.py (superseded).

0021-pyobjc-desktop-addon-deps.patch — pyproject: pyobjc-framework-{Quartz,Cocoa,ApplicationServices} (darwin) for the desktop_use.py addon; the deps-only runtime install (app #45) reads [project].dependencies, so addon deps must live here.

0022-deepseek-vision-default.patch — agent/auxiliary_client.py: add "deepseek": "deepseek-flash" (natively multimodal V4.1-Flash; replaced the retired deepseek-v4-flash-vision-exp on 2026-09-10) to _PROVIDER_VISION_MODELS (upstream ships only xiaomi/zai and DeepSeekProfile has no default_vision_model()). Without it a DeepSeek main provider — or auxiliary.vision.provider: deepseek with no model — resolves vision to the image-less chat model, so check_vision_requirements() is False and vision_analyze/video_analyze/browser_vision are never advertised. Pairs with the app-side auxiliary.vision pin (lucaryin-ai electron/provisioning/runtime-config-pins.ts) for fleet boxes that name no main provider.

0025-lucaryin-child-env-strip.patch — tools/environments/local_env_policy.py: _is_hermes_internal_secret also covers the bridge worker's own credentials (BRIDGE_AUTH_TOKEN, LUCARYIN_API_KEY, VOICE_JWT_SECRET, IMESSAGE_PASSWORD, ASSEMBLYAI_API_KEY, FLEET_TOKEN, FLEET_EMAIL_PASSWORD, PLAID_ACCESS_TOKENS — exact names), so no terminal/background/PTY/execute_code child sees them (R2-1-03, HA3); tools/browser_tool.py reads the bridge bearer file-then-env through tools/bridge_auth.py. Pairs with lucaryin-ai B1 (bridge.token publisher).

0026-cron-oneshot-late-fire-window.patch — cron/jobs.py: one-shot late-fire window (30 min default, meeting_join until the meeting ends, late_fire_window_seconds override) instead of the 120 s grace, and a one-shot past its window is kept only for a LIVE claim; cron/scheduler.py: tick() submits one-shots before recurring jobs (R2-1-43, HA4). Pairs with lucaryin-ai B5 (serial ticks, cron_tick_support.py).

0009 now also touches tools/browser_tool_eval_policy.py (browser_console), tools/browser_use_cli.py (browser_exec) and tools/browser_extension_router.py (the extension route); the shared session-material contract is tools/session_material_policy.py (verbatim copy of lucaryin-ai ui_access_policy + approval_gate). 0008 denies are case/normalization-folded on macOS/Windows, fold the /System/Volumes/Data firmlink prefix (macOS) and the \\?\ / \\?\UNC\ prefixes (Windows), and ALSO compare by file identity (st_dev, st_ino of the checked path's existing ancestors vs each deny base, base stats cached 2 s) so firmlink, hardlink, admin-share and 8.3 spellings hit the same denies; they cover ~/.lucaryin/auth (bridge.token) and a sibling profile's config.yaml / state.db / sessions.

tools/email_send_tool.py dedup results follow the EMAIL_SEND RESULT CONTRACT shared with lucaryin-ai (the bridge worker/server read it): identical message = {sent:true, already_sent:true, deduplicated:true, match:"message", idempotent_skip:true, summary}; cron thread hold = {sent:false, held:true, deduplicated:true, match:"thread", reason, summary}. Change both repos together.

tools/gbrain_tool.py signs every :9050 call (gbrain_search, gbrain_read; the /health probe too) with the bridge bearer through tools/bridge_auth.py, file then env. Pairs with lucaryin-ai B17 (#109, R2-1-23), which makes the gbrain bridge refuse every route but GET /health without it: B17 without this = gbrain_search answers "not reachable" and gbrain_read "no compiled page" on every call (401s); this without B17 = a header the old bridge ignores. A 401/403 reads as recall unavailable, never as "no compiled page". runtime-addons/tests/tools/test_gbrain_tool_bearer.py also holds every addon that names a loopback address to tools/bridge_auth.

TESTS: runtime-addons/tests/conftest.py is the addon suite's hermetic guard (tmp HOME/HERMES_HOME/LUCARYIN_AUTH_DIR per test; an audit hook refuses and fails any access to the real ~/.hermes / ~/.lucaryin). build-runtime.sh installs it as build/runtime/conftest.py (never over upstream's tests/conftest.py) with build/runtime/.lucaryin-addon-tests listing the addon test files it applies to, and fails the build if any other addon file would replace an upstream or patched file.
